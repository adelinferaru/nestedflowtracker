includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: 5
    paths:
        - src
    excludePaths:
        # Config files are declarative data; env() is legitimate inside them.
        - src/config/*
    # tmpDir keeps PHPStan's cache out of the project root.
    tmpDir: build/phpstan
